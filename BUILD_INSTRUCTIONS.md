# Инструкции по сборке приложений

## ✅ Windows версия - ГОТОВА!

Windows версия уже собрана и находится в папке:
```
dist/NanoBananaPro.exe
```

**Размер:** ~159 MB

**Готово к использованию!** Просто скопируйте файл `NanoBananaPro.exe` и запустите его на любой Windows системе (даже без установленного Python).

---

## 🍎 macOS версия - Требуется сборка на macOS

**ВАЖНО:** macOS приложения можно собрать **только на macOS системе**. Это ограничение Apple и PyInstaller.

### Вариант 1: Автоматическая сборка (рекомендуется)

Если у вас есть доступ к macOS (MacBook, iMac, или виртуальная машина):

1. Скопируйте весь проект на macOS систему
2. Откройте Terminal
3. Перейдите в папку проекта:
   ```bash
   cd /path/to/Generate\ Nanobanana\ Pro
   ```
4. Сделайте скрипт исполняемым и запустите:
   ```bash
   chmod +x build_macos.sh
   ./build_macos.sh
   ```

Или просто запустите:
```bash
python3 build.py
```

Результат будет в `dist/NanoBananaPro.app`

### Вариант 2: Ручная сборка на macOS

```bash
# 1. Установите зависимости
pip3 install -r requirements.txt

# 2. Запустите сборку
python3 build.py
```

### Вариант 3: Использование GitHub Actions (для автоматизации)

Если у вас есть репозиторий на GitHub, можно настроить автоматическую сборку для обеих платформ через GitHub Actions. Это позволит собирать приложения автоматически при каждом коммите.

---

## 📦 Создание DMG для macOS (опционально)

После сборки `.app` файла, можно создать DMG образ для удобного распространения:

```bash
# Установите create-dmg
brew install create-dmg

# Создайте DMG
create-dmg --volname "NanoBanana Pro" \
           --window-pos 200 120 \
           --window-size 800 400 \
           --icon-size 100 \
           --app-drop-link 600 185 \
           "NanoBananaPro.dmg" \
           "dist/NanoBananaPro.app"
```

---

## 🧪 Тестирование

Перед распространением обязательно протестируйте:

### Windows:
1. Скопируйте `NanoBananaPro.exe` на чистую Windows систему (без Python)
2. Запустите и проверьте все функции
3. Убедитесь, что:
   - Приложение запускается
   - Генерация изображений работает
   - Редактирование работает
   - Комбинирование работает
   - Галерея отображается
   - База данных создается и работает
   - Изображения сохраняются

### macOS:
1. Скопируйте `NanoBananaPro.app` на чистую macOS систему
2. Возможно потребуется разрешить запуск в настройках безопасности
3. Проверьте все функции как для Windows

---

## 📋 Текущий статус

- ✅ **Windows:** Собрано и готово (`dist/NanoBananaPro.exe`)
- ⏳ **macOS:** Требуется сборка на macOS системе

---

## 💡 Альтернативные решения для macOS

Если у вас нет доступа к macOS:

1. **Виртуальная машина:** Установите macOS в виртуальной машине (VMware, VirtualBox)
2. **Облачные сервисы:** Используйте облачные Mac-серверы (MacStadium, AWS Mac instances)
3. **GitHub Actions:** Настройте автоматическую сборку через GitHub Actions (бесплатно для публичных репозиториев)
4. **Попросите друга:** Если у кого-то есть Mac, они могут собрать приложение за вас

---

## 🔧 Устранение проблем

### Windows сборка не работает:
- Убедитесь, что Python 3.8+ установлен
- Установите зависимости: `pip install -r requirements.txt`
- Проверьте, что приложение запускается: `python main.py`

### macOS сборка не работает:
- Убедитесь, что используете Python 3.8+
- Проверьте права доступа: `chmod +x build_macos.sh`
- Установите зависимости: `pip3 install -r requirements.txt`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи сборки
2. Убедитесь, что все зависимости установлены
3. Попробуйте пересобрать с параметром `--clean`





